# Bind web port to loopback only (no LAN/public access).
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build

services:
  web:
    ports: !override
      - "127.0.0.1:8005:80"
